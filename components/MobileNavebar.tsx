import {
  Sheet,
  SheetContent,
  SheetDescription,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from '@/components/ui/sheet'

const MobileNavebar = ({ user }: MobileNavProps) => {
  return <div>MobileNavebar</div>
}
export default MobileNavebar
